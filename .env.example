# Copy this file to .env.local and fill in real values from the Google Cloud
# console (APIs & Services -> Credentials -> your Desktop-app OAuth client).
# .env.local is gitignored so the secret never enters version control.
VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_CLIENT_SECRET=
